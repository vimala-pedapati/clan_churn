import 'package:flutter/material.dart';

class EditClientDetails extends StatelessWidget {
  const EditClientDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}